{
  "name": "hedra-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for Hedra API",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
